function helloString(name) {
    //This function has to return a greeting to the name passed as parameter. EX: "Hello Pah"
}

function equalStrings(string1, string2) {
    //This function has to return if the strings are equal
}

function countingWords(string) {
    //This function has to return how many words 
}

function amazingHacker(string, oldWord, newWord) {
    //This function has to replace the old word with the new word.
}

module.exports = {
    helloString,
    equalStrings,
    countingWords,
    amazingHacker
}